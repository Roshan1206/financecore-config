server:
  port: 8090
  servlet:
    context-path: /api

auth:
  config:
    database:
      schema: fc_auth
    auth:
      url: http://localhost:${server.port}
      client:
        id: auth-server
        secret: auth-secret

spring:
  application:
    name: auth
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_core?currentSchema=${auth.config.database.schema}
    username: postgres
    password: password
  jpa:
    properties:
      hibernate:
        default_schema: ${auth.config.database.schema}
    hibernate:
      ddl-auto: validate
  security:
    user:
      name: fincancecore
      password: password

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
